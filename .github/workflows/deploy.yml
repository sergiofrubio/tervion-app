# name: Despliegue en Servidor Local

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: self-hosted
#     steps:
#       - name: Descargar código
#         uses: actions/checkout@v4

#       - name: Levantar contenedor con Docker
#         run: |
#           docker compose down
#           docker compose build --no-cache
#           docker compose up -d