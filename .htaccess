RewriteEngine On

# Si el archivo físico solicitado existe (ej. /public/custom/css/app.css), servirlo directamente
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Si no es un archivo físico existente, pasar la petición a public/index.php
RewriteRule ^ public/index.php [L,QSA]

